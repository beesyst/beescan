# 🛡️ BeeScan

**BeeScan** — модульная платформа для комплексного аудита безопасности IT-инфраструктуры. Поддерживает подключение внешних инструментов в виде плагинов, автоматический сбор и структурирование результатов, генерацию отчетов в различных форматах и гибкую централизованную настройку. Архитектура BeeScan позволяет анализировать сеть, веб-приложения, DNS и API, а также масштабировать систему под любые задачи DevSecOps, пентеста и мониторинга.

## Основные возможности

- **Поддержка плагинов** — 5 инструментов.  
- **Plug-and-Play архитектура** — каждый инструмент вынесен в отдельный модуль-парсер.  
- **Вывод в PostgreSQL** и рендер отчетов из БД.  
- **Тул-профили** — выбирай уровень обработки сканера в каждом инструменте.  
- **Генерация отчетов**: TERMINAL, HTML, PDF.  
- **Изоляция через Docker** — отдельные контейнеры для ядра и БД.  
- **Логирование** — отдельные логи для хоста и контейнера.
- **Мультиязычность** - бот поддерживает **переключение языка** через `config.json` и добавление новых языков в `lang.json`.

## Где можно использовать

- **Пентесты и тесты на проникновение**  
- **Аудит инфраструктуры и веб-сервисов**  
- **Безопасность государственных и корпоративных сетей**  
- **DevSecOps и CI/CD**

## Технологический стек

- **Python** — основной язык разработки  
- **PostgreSQL** — база данных  
- **Docker** — контейнеризация среды  
- **Jinja2** — шаблоны отчетов  
- **Rich** — таблицы в терминале  
- **WeasyPrint** — генерация PDF

### Подключенные инструменты

| Инструмент | Описание | Версия |
|------------|----------|--------|
| `nmap`     | Сканирование сети, обнаружение портов, сервисов и версий. | ![v](https://img.shields.io/badge/nmap-stable-blue) |
| `nikto`    | Анализ веб-серверов, поиск конфигурационных ошибок и уязвимостей. | ![v](https://img.shields.io/badge/nikto-2.5.0-blue) |
| `dig`      | Получение и анализ DNS-записей, определение зоны ответственности. | ![v](https://img.shields.io/badge/dig-bind9-blue) |
| `nuclei`   | Проверка на наличие шаблонных уязвимостей по YAML-шаблонам. | ![v](https://img.shields.io/badge/nuclei-v3.4.3-blue) |

## Архитектура

### Компоненты системы

1. **Плагины (`plugins/*.py`)** — модули-обертки над CLI-инструментами (например, `nmap`, `nikto`). Каждый плагин реализует функции:
   - `scan()` — запуск сканера и сохранение пути к файлу результата (`.xml`, `.json`);
   - `parse()` — парсинг результатов;
   - `merge_entries()` — объединение данных по IP и Domain в `source: "Both"`;
   - `get_column_order()` и `get_wide_fields()` — настройка визуального отображения.
2. **Runner (`plugin_runner.py`)** — запускает плагины, сохраняет пути к их результатам во временный JSON (`/tmp/temp_files_*.json`), не сохраняет содержимое файлов.
3. **Коллектор (`collector.py`)** — загружает пути файлов, вызывает `parse()` / `merge_entries()`, фильтрует неинформативные строки и сохраняет результат в таблицу `results`.
4. **База данных (PostgreSQL)** — централизованное хранилище для всех результатов, все данные приводятся к единой схеме, сохраняются по таблицам.
5. **Генератор отчетов (`report_generator.py`)** — извлекает данные из БД, группирует по категориям, визуализирует:
   - терминальный отчет,
   - HTML,
   - PDF.
6. **Конфигурационный модуль (`config/config.json`)** — определяет цели сканирования (`target_ip`, `target_domain`), активные плагины, уровни сложности, формат отчетов, тему (`light` / `dark`) и поведение (`open_report`, `clear_db` и др.).
7. **Оболочка запуска (`start.py`)** — единая точка входа, управляет запуском Docker-среды, БД, сканеров, сбором и генерацией отчетов с прогресс-индикаторами.
8. **Docker-среда** — изолированное и полностью самодостаточное окружение:
   - `beescan_base` — контейнер со всеми сканерами и логикой,
   - `postgres` — отдельный контейнер с базой данных,
   - `beescan_network` — мостовая сеть между компонентами.

### Структура проекта

``` 
beescan/
├── config/                    # Конфигурация
│   ├── plugins/
│   │   ├── nikto.json         # Профиль и уровни для Nikto
│   │   └── nmap.json          # Профиль и уровни для Nmap
│   ├── config.json            # Главный конфиг BeeScan (цели, плагины и т.д.)
│   └── start.py               # Точка запуска, CLI-оболочка для старта системы
├── core/                      # Основная система
│   ├── collector.py           # Парсер и интегратор в БД
│   ├── logger_container.py    # Логгер контейнера (core-уровень)
│   ├── logger_host.py         # Логгер хоста (core-уровень)
│   ├── logger_plugin.py       # Логгер для отдельных плагинов
│   ├── orchestrator.py        # Управление зависимостями и порядком запуска
│   ├── plugin_runner.py       # Запуск всех плагинов и запись temp-файлов
│   ├── registry.py            # Работа с таблицей registry (очередь целей)
│   ├── report_generator.py    # Генератор отчетов (terminal, html, pdf)
│   └── severity.py            # Уровни критичности и классификация
├── db/                        # Настройка БД
│   ├── compose.yaml           # Docker Compose для PostgreSQL
│   ├── Dockerfile             # Dockerfile (опционально)
│   ├── init.sql               # Инициализация структуры БД
│   └── populate_db.py         # Заполнение тестовыми данными
├── docker/                    # Настрйока Docker
│   ├── Dockerfile.base        # Dockerfile для beescan-base (сканеры, Python)
│   └── install_plugins.py     # Скрипт установки инструментов в контейнер
├── logs/                      # Логирование
│   ├── container.log          # Лог контейнера
│   ├── host.log               # Лог хоста
│   ├── nikto.log              # Лог Nikto
│   └── nmap.log               # Лог Nmap
├── plugins/                   # Сканер-парсеры 
│   ├── dig.py                 # Плагин для dig
│   ├── nikto.py               # Плагин для Nikto
│   ├── nmap.py                # Плагин для Nmap
│   └── nuclei.py              # Плагин для nuclei
├── reports/                   # Сгенерированные отчеты
│   ├── *.html
│   ├── *.json
│   └── *.pdf
├── templates/                 # Шаблоны отчетов Jinja2
│   ├── css/                   # Стили
│   ├── plugins/               # Подшаблоны для html-отчетов каждого плагина
│   │   ├── nmap.html.j2              
│   └── report.html.j2         # Основной HTML-шаблон
├── requirements.txt           # Python requirements
├── start.sh                   # Bash-стартер для всей системы
└── version.txt                # Версия релиза
```
## Pipeplan: Как это работает?

### Запуск системы

1. Система запускается через `start.sh`. Bash-обертка проверяет окружение, запускает `start.py` и orchestration пайплайн.
2. Проверяется наличие Docker и сети `beescan_network`.
3. Запуск контейнеров:
   - PostgreSQL (beescan_postgres)
   - beescan-base (ядро + сканеры)
4. Запускается `plugin_runner.py` для асинхронного сканирования целей.
5. Сохраняются пути к результатам сканирования во временный файл `/tmp/temp_files_*.json`.
6. Запускается `collector.py`, который парсит, нормализует, фильтрует и записывает результаты в БД (строго по схеме snapshot).
7. Генерируются отчеты: `terminal`, `html`, `pdf` - на основе данных из БД через `report_generator.py`.

### Работа плагинов (`plugin_runner.py`)

1. Определяется активность через `config.json`.
2. Асинхронно вызывается функция `scan()`:
   * Запускает сканер (например, nmap) с нужным профилем/аргументами.
   * Сохраняет результаты (XML, JSON, STDOUT) во временные файлы.
3. Временные файлы не читаются напрямую и их пути сохраняются во временный JSON.
4. Далее работает только коллектор.

### Сбор данных (`collector.py`)

1. Коллектор подключается к БД.
2. Загружает плагин-парсер из `plugins/*.py`.
3. Обрабатывает все временные файлы (`temp_files_*.json`):
   * вызывает `parse()` для разбора результатов,
   * вызывает `merge_entries()` для унификации дублей,
   * фильтрует неинформативные записи
4. Все валидные записи раскладываются по таблицам:
   * **hosts** — уникальные IP/FQDN и OS.
   * **services** — уникальные сервисы (порт, протокол, CPE, продукт и т.д.).
   * **vuln** — результат для каждой сервисной связки (всегда содержит severity, description, references и т.д.).
   * **evidence** — при необходимости/запросе сохраняются оригинальные логи (например, XML или stdout).
   * **registry** — динамический индекс целей для пассивных/последующих плагинов.

### Единая схема хранения

* Любой результат плагина всегда раскладывается по универсальным полям: `host`, `service`, `port`, `protocol`, `severity`, `description`, `evidence`, `references`, `source`.
* Если плагин отдает что-то специфичное — оно попадает в поля `meta`, `evidence`, либо отдельное расширенное описание.
* Сырые данные (`evidence`) опциональны: для nmap можно сохранить исходный XML/STDOUT в evidence, для nuclei — JSON-отчет и т.д.

### Генерация отчетов (`report_generator.py`)

1. Извлекает свежие данные из snapshot: таблицы `hosts`, `services`, `vuln`, `evidence`, `registry`.
2. Автоматически группирует данные по категориям и активным плагинам на основе `config.json` (`category` для каждого плагина).
3. Для каждого плагина запрашивает у него:
   * порядок колонок (`get_column_order()`)
   * расширенные поля (`get_wide_fields()`)
   * вид (`get_view_rows()`)
4. Генерирует отчеты в выбранных форматах:
   * **Терминальный отчет** (`rich`-таблицы, полный вывод по плагинам)
   * **HTML-отчет** (через универсальный Jinja2-шаблон `report.html.j2`, который подстраивается под структуру каждого плагина)
   * **PDF-отчет** (генерируется из HTML через WeasyPrint)
5. Поддерживается выбор темы отчета (`"light"` или `"dark"`).
6. Все отчеты формируются только на основе актуального состояния БД, без промежуточных JSON/логов. 

### Пример работы Nmap

1. Включен модуль `nmap`, выставлен профиль сложности (`easy` / `middle` / `hard`).
2. Для каждого target (ip/domain/network) запускается свой профиль nmap с нужными опциями (аргументы, порты, скрипты).
3. Каждый результат сохраняется отдельно, далее собирается, разбирается и нормализуется через функции плагина (`parse()`, `merge_entries()`, `get_view_rows()`)
4. Все данные строго попадают в snapshot:
   * сервисы (`services`),
   * хосты (`hosts`),
   * отдельные findings (`vuln`),
   * при необходимости сырые XML (`evidence`)
5. В отчетах виден весь объем:
   * **Терминал** — агрегированный вывод по всем сервисам и severity.
   * **HTML/PDF** — табличный, с сортировкой, фильтрацией по категориям/плагинам, временем сканирования и поддержкой тем.

## Установка и запуск

### Запуск проекта

```bash
git clone https://github.com/beesyst/beescan.git
cd beescan
bash start.sh
```

В процессе установки нужно будет выбрать язык установки.

## Настройка конфигурации

Все параметры задаются в файле `config.json`:

| Параметр              | Значение по умолчанию      | Что делает `true`/значение                          | Что делает `false`/другое значение            |
|-----------------------|---------------------------|-----------------------------------------------------|-----------------------------------------------|
| `target_ip`           | `"1.1.1.1"`               | Сканирует указанный IP-адрес                        | —                                             |
| `target_domain`       | `""`                      | Сканирует указанный домен                           | —                                             |
| `target_network`      | `""`                      | Сканирует указанный диапазон (например, `1.1.1.0/24`)| —                                            |
| `target_api`          | `""`                      | Для интеграции с внешним API (опционально)          | —                                             |
| `report_formats`      | `["terminal", "html"]`    | Генерирует выбранные форматы                        | —                                             |
| `open_report`         | `true`                    | Авто-открытие HTML/PDF-отчёта                       | Не открывает отчёты в браузере                |
| `clear_logs`          | `true`                    | Очищает логи перед запуском                         | Логи накапливаются                            |
| `clear_reports`       | `true`                    | Очищает старые отчёты перед запуском                | Старые отчёты сохраняются                     |
| `clear_db`            | `true`                    | Очищает все таблицы базы перед запуском             | Старые данные в базе сохраняются              |
| `report_theme`        | `"dark"`                  | Используется тёмная тема отчёта                     | `"light"` — светлая тема                      |
| `plugins`             | см. секцию ниже           | Список активных плагинов и их параметры             | —                                             |

## Донат

- **USDT (TRC20)**/**USDC (TRC20)**: `TUQj3sguQjmKFJEMotyb3kERVgnfvhzG7o`
- **SOL (Solana)**: `6VA9oJbkszteTZJbH6mmLioKTSq4r4E3N1bsoPaxQgr4`
- **XRP (XRP)**: `rDkEZehHFqSjiGdBHsseR64fCcRXuJbgfr`

---

**Licensed for non-commercial use only. See [LICENSE](LICENSE) for details.**